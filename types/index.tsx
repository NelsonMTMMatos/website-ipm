export interface Report {
    description: string;
    availability: boolean;
    stage_assigment: string;
};

export interface Person {
    name: string;
    number:number;
    photo: string;
    assignment: string;
}